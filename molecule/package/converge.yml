---
- name:                 Converge
  hosts:                all
  vars:
    pip_install_method: package
    pip_packages:
      - ansible
      - ansible-lint
      - molecule[docker]
  tasks:
    - name:             "Include ansible-role-pip"
      include_role:
        name:           "ansible-role-pip"
